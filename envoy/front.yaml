dynamic_resources:
    lds_config:
      path: "/etc/envoy/lds.yaml"
    cds_config:
      path: "/etc/envoy/cds.yaml"
  admin:
    access_log_path: "/dev/null"
    address:
      socket_address:
        address: 0.0.0.0
        port_value: 8001